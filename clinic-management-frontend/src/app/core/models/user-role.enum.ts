// src/app/core/models/user-role.enum.ts

/**
 * @description
 * Enum defining standard user roles within the application.
 */
export enum UserRole {
  ADMIN = 'admin',
  DOCTOR = 'doctor',
  PATIENT = 'patient',
  NURSE = 'nurse',
  STAFF = 'staff',
  USER = 'user' // Generic user role
}