// src/app/core/models/api-status.enum.ts

/**
 * @description
 * Enum defining common API response status indicators.
 */
export enum ApiStatus {
  SUCCESS = 'SUCCESS',
  FAILURE = 'FAILURE',
  PENDING = 'PENDING',
  NOT_FOUND = 'NOT_FOUND',
  UNAUTHORIZED = 'UNAUTHORIZED'
}