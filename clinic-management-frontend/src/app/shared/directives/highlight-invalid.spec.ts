import { HighlightInvalid } from './highlight-invalid';

describe('HighlightInvalid', () => {
  it('should create an instance', () => {
    const directive = new HighlightInvalid();
    expect(directive).toBeTruthy();
  });
});
